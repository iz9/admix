import type { ThemeUICSSObject } from 'theme-ui'

export const svgContainerStyles: ThemeUICSSObject = {
  alignItems: 'center',
  display: 'inline-flex',
  height: 'auto',
  justifyContent: 'center',
  width: 'auto',
}
